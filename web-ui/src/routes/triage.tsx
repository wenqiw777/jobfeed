import { ZonePlaceholder } from "./placeholder";

export default function TriagePage() {
  return (
    <ZonePlaceholder
      zone="Triage"
      hint="Today's queue lands here: scan results sorted by verdict, decided with the keyboard."
    />
  );
}
