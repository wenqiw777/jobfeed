import { ZonePlaceholder } from "./placeholder";

export default function SourcesPage() {
  return (
    <ZonePlaceholder
      zone="Sources"
      hint="ATS company coverage: paste a list, probe vendors, confirm, and scan picks them up."
    />
  );
}
