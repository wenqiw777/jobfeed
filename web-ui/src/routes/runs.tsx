import { ZonePlaceholder } from "./placeholder";

export default function RunsPage() {
  return (
    <ZonePlaceholder
      zone="Runs"
      hint="Pipeline run history with per-run counters. Runs are triggered from the CLI."
    />
  );
}
