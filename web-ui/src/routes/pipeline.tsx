import { ZonePlaceholder } from "./placeholder";

export default function PipelinePage() {
  return (
    <ZonePlaceholder
      zone="Pipeline"
      hint="Applications in flight: follow-ups due, interview prep, and ghosting watch."
    />
  );
}
