import { ZonePlaceholder } from "./placeholder";

export default function LibraryPage() {
  return (
    <ZonePlaceholder
      zone="Library"
      hint="Every posting ever seen: searchable, sortable, with full evaluation detail."
    />
  );
}
