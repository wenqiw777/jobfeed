import { ZonePlaceholder } from "./placeholder";

export default function InsightsPage() {
  return (
    <ZonePlaceholder
      zone="Insights"
      hint="The funnel at a glance: discovered through applied, by day and by resume."
    />
  );
}
