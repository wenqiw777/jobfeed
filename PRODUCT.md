# Product

## Register

product

## Users

主用户是工具作者本人 —— 一名 UMich 计算机工程在读、做 production LLM 系统的准毕业 SWE 候选人,正在求职。使用场景:每天在本地打开,对当天抓取到的职位做 triage(看 / 决策 / 跳过),把"值得投"的推进到申请流程,并追踪已投职位的后续状态(面试、跟进、ghost)。

他是这个领域的熟练用户,不需要被引导,要的是**快、顺手、不打断心流**。定位是"自用为底、可展示为面":每天好用是底线,但成品要做到能截图进 portfolio、面试时 screen-share、甚至开源公开的水准 —— 所以陌生人也可能成为次要受众(看界面、读代码)。

## Product Purpose

jobfeed 是一个本地优先的求职指挥台:抓取多源职位 → 两阶段 LLM 评估打分 → triage 队列 → 申请与简历快照审计 → 跟进与统计。Web UI 不是一张职位表格,而是把"今天该看什么、该决策什么、投了什么、接下来追什么"收敛成一个可执行的工作流。

成功的样子:打开就知道今天该处理哪几条,决策一条自动到下一条,投递留痕可查,数据一眼看清漏斗 —— 整个过程像在用一件趁手的精密工具,而不是在填一个后台系统。

## Brand Personality

**精密工具感(Linear / Raycast 那一档)。** 三个词:克制、利落、可信。

- **克制**:状态色只在该出现时出现,不靠颜色和装饰刷存在感;留白和层级承担表达,而非边框和卡片堆叠。
- **利落**:键盘驱动、决策→下一项的 inbox 节奏(Today 页的灵魂来自 Superhuman 式 triage);响应快,动效只传达状态、150–250ms,不表演。
- **可信**:数据密集处(stats / 申请流水)专业沉稳,像 Stripe / Vercel 仪表盘那样让人信任,而非花哨。

工具应当消失在任务里。熟练用户坐下来就信任每一个控件,不会在任何一个"略微不对劲"的组件上停顿。

## Anti-references

明确不要像:

- **消费级花哨**:大圆角、弹跳 / 橡皮筋动效、emoji 装饰、糖果色。可爱但不专业、不像工具。
- **设计喧宾夺主**:炫技动效、display 字体当 UI label、为好看牺牲效率。(impeccable product ban)
- **企业级后台**:密密麻麻的灰色表格、Bootstrap / AdminLTE 既视感、沉闷无品味。
- **(隐含)随手 SaaS 模板**:紫蓝渐变、卡套卡、彩底灰字、每节一个小写 eyebrow、hero 大数字模板 —— impeccable 的 absolute bans 已覆盖,默认避开。

**二阶警惕**:不要落入"求职 / AI 工具 → 默认抄 Linear 深色 + 那套 SaaS 灰"的反射。Linear 是**气质**参照,不是配色拷贝;差异化在 DESIGN.md 解决。

## Design Principles

1. **任务消失术(the tool disappears)** —— 每个屏幕只服务当下那一个主任务(triage 决策 / 推进申请 / 看漏斗)。不确定一个元素是否帮到当下任务,就删掉它。
2. **决策→下一项的心流** —— 核心节奏是"看一条、决策、自动到下一条",像 inbox-zero。任何打断这个节奏的(模态、确认弹窗、加载空白)都要先穷尽内联 / 渐进替代。
3. **克制即可信** —— 颜色、动效、装饰都按需出现;密度服务信息而非堆砌。可信感来自一致性和精度,不来自视觉音量。
4. **能展示的底层是真好用** —— "可展示"不是加一层皮,而是把每天真实用到的流程打磨到无瑕。截图好看是好用的副产品。
5. **一致优先于惊喜** —— 同一个动作在哪都长一样(同样的按钮、表单控件、图标语言);delight 留给少数关键时刻,不铺满每个页面。

## Accessibility & Inclusion

当前阶段**视觉优先,a11y 不作为硬性合规门槛**(用户明确选择)。但两条因为"本就是好设计"而保留:

- **对比度达标**:正文 ≥4.5:1、大字(≥18px 或 bold ≥14px)≥3:1。当"可读性"做、不当合规做 —— 顺带消除现有 warm off-white 背景上灰字偏淡的隐患(impeccable 点名的头号 AI 可读性问题)。
- **键盘可达**:triage 本就是键盘流,顺势保证主路径 tab 可达、focus 可见。

**已知欠账(日后补,先标注、不假装)**:全面 WCAG AA 审计、reduced-motion 全覆盖、屏幕阅读器语义、色盲友好校验。等真正公开 / 开源时再正式补齐。
